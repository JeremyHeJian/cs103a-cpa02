<ul>
    <% for (repo of repos){%>
        <li> 
            <a href="/saved/remove/<%= repo._id %>">X</a>
            <%= repo.repo_name %>
            <%= repo.language %>
            <%= repo.username %>
        </li>
    <% } %>
</ul>

<!--
    <pre>
        <%= JSON.stringify(repos,null,5) %>
    </pre>
-->